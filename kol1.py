import random
correctAngle = 10;

While True:
	#check if there was a loop break e.g. by interruption handler,  
	currentAngle = rand();	#not sure how to call rand function
	correction = correctAngle - currentAngle
	print("Current orientation: " + str(currentAngle) + " degrees");
	print("Airplane has to be tilted by " + str(correction) + " degrees"); 
